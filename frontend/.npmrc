# Group shared SVG package will be installed from GitHub Packages once GITHUB_TOKEN is in env.
# Until then, src/components/icons/Icon.vue ships an inline fallback covering the same icon set.
@freeco-company:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
